name: TechMarket Orders - Examen Transversal

on:
  workflow_dispatch: 

jobs:
  run-build-template:
   
    uses: ./.templates/build-template.yml@main
    with:
      image_tag: "et-2026-release"

  run-deploy-and-remediation:
    needs: run-build-template
    
    uses: ./.templates/deploy-template.yml@main
    with:
      target_env: "green"
    secrets: inherit 
